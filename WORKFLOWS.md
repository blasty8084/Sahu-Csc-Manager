# SAHU CSC — WORKFLOWS.md (superseded)

This file has been consolidated into `replit.md` to remove duplication. It is kept only as a pointer so old links/bookmarks don't 404.

- **Workflow list, ports, run commands, first-time setup, credentials, troubleshooting** → see the "Replit Setup" and "Workflows" sections of [`replit.md`](./replit.md)
- **Architecture / monorepo layout** → see [`architectureV3.md`](./architectureV3.md)

`replit.md` is the single source of truth for how to start, seed, and troubleshoot the SAHU CSC platform going forward.
